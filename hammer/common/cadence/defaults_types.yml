# Common Cadence settings.
cadence:
  # Directory where Cadence tools are installed. Contains subfolders like
  # INNOVUS, GENUS, etc.
  cadence_home: str

  # License server information, to be overridden by some project generator.
  # e.g. "port_a@server:port_b@server"
  CDS_LIC_FILE: str

  # Extra environment variables.
  # List of dictionaries of variable-values.
  # e.g. "MY_VAR=foo" -> [ {"MY_VAR": "foo"} ]
  extra_env_vars: list[dict[str, str]]
